package cz.cvut.fel.smarthome.model.entities;

public enum State {
    S_OPENED,
    S_CLOSED,
    S_OFF,
    S_ON,
    S_LIGHT,
    S_DARK,
    S_IDLE,
    S_PLAYING,
    S_BROKEN,
    S_AVAILABLE,
    S_NOT_AVAILABLE,
    S_OVERLOAD,
    S_NORMAL
}
